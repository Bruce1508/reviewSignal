"""evaluation runs: record the benchmark's taxonomy version as provenance

Revision ID: c4a17b9d2e60
Revises: 3f8513e22ef8
Create Date: 2026-09-06 15:10:00.000000

`taxonomy_version_id` is a foreign key, so it is NULL for every Phase 0 run: no
`taxonomy_versions` rows exist yet. The benchmark file names its taxonomy as text,
and that string was being dropped on persist, leaving a run unable to say what its
gold labels were made against (`evaluation.md` §30, and §23 regression comparison).
Written by hand rather than autogenerated: see revision 3f8513e22ef8 on why
autogenerate re-proposes the `use_alter=True` foreign keys.
"""

from collections.abc import Sequence

import sqlalchemy as sa
from alembic import op

revision: str = "c4a17b9d2e60"
down_revision: str | None = "3f8513e22ef8"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    op.add_column(
        "evaluation_runs",
        sa.Column("taxonomy_version", sa.String(length=64), nullable=True),
    )


def downgrade() -> None:
    # Provenance only; no other column is derived from it, so dropping it loses
    # exactly the history this revision added.
    op.drop_column("evaluation_runs", "taxonomy_version")
